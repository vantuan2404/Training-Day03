import { AppPricePipePipe } from './app-price-pipe.pipe';

describe('AppPricePipePipe', () => {
  it('create an instance', () => {
    const pipe = new AppPricePipePipe();
    expect(pipe).toBeTruthy();
  });
});
